/**
 * Renders the data spreadsheet view.
 */
export function Spreadsheet() {
    // We will connect this to Redux later
    return (
      <div className="w-full h-full p-8 bg-gray-50">
        <h1 className="text-2xl font-bold">Spreadsheet View</h1>
        <p className="mt-2">The data grid will be displayed here.</p>
      </div>
    );
  }